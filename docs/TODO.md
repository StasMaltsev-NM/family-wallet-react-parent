# Family Wallet TODO

Дата обновления: 2026-02-14

## Приоритет
- [x] Картинки в карточках наград у детей должны дублироваться из родительского приложения.
- [x] Удаление/скрытие выполненных миссий у детей (сейчас видны серым списком).
- [x] Убрать кнопку `Выход` у детей с главного нижнего бара.
- [x] Перенести `Выход` во вкладку `Я` и расположить в самом низу.
- [x] После создания профиля ребёнка у родителя показывать модальное окно с кодом ребёнка:
  - Текст: `Введите этот код в приложении ребенка @family_wallet_kids_bot на его телефоне`.
  - Кнопка `Скопировать`.
- [x] Если ребёнок один, выбирать его автоматически при создании миссий и наград.
- [x] В форме миссий у родителя кнопка должна быть `Создать` (без `Создать для 1/2/3`).
- [x] В новом аккаунте родителя должна быть кнопка `Перегенерировать картинку` у наград (проверить паритет функционала).

## Техническая проверка
- [x] Проверить, почему в новом аккаунте родителя нет кнопки перегенерации (ветка/деплой/кеш/роль).
- [x] Добавить регрессионный чеклист перед деплоем для родительского и детского приложений.

## Финальный этап оптимизации графики
- [ ] После исправления критичных ошибок перевести графический контент на оптимизированные форматы:
  - Генерируемые изображения наград: хранение/отдача в `webp`.
  - Иконки интерфейса: использовать `svg`, где это возможно.
- [ ] Добавить backend-оптимизацию: изображения наград, которые сейчас приходят в `jpeg`, конвертировать в `webp` перед сохранением/отдачей в магазин.
